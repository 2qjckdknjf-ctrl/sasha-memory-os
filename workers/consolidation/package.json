{
  "name": "@memory-os/worker-consolidation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "consolidate:once": "tsx src/index.ts",
    "consolidate:loop": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@memory-os/db": "workspace:*",
    "@memory-os/domain": "workspace:*",
    "@memory-os/retrieval": "workspace:*"
  }
}
