# Copy to .env for local use. Never commit real values.
# See docs/engineering/SECRETS_POLICY.md

MEMORY_OS_ENV=local

# Supabase (dedicated Memory OS project — not AISTROYKA/HiAir)
MEMORY_OS_SUPABASE_URL=
MEMORY_OS_SUPABASE_ANON_KEY=
# Server-only — never expose to Web/PWA/companion
MEMORY_OS_SUPABASE_SERVICE_ROLE_KEY=
