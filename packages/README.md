# Packages

Shared TypeScript libraries. Prefer package public APIs over deep imports.

Notable:
- `@memory-os/db` — Supabase RPC gateway used by API and MCP
- `@memory-os/ingestion` — document parsers (TXT/PDF/DOCX) and chunking
